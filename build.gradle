import java.text.DateFormat
import java.text.SimpleDateFormat


buildscript {
    repositories { 
      mavenCentral()
		  mavenLocal()
		  jcenter()
    }
    dependencies {
classpath group: 'org.whitesource', name: 'whitesource-gradle-plugin', version: '17.11.3'
    }
}

configurations {
	compile
	runtime
}

repositories {
    mavenCentral()
}

dependencies {
	compile group: 'org.kohsuke', name: 'github-api', version: '1.92'
} 

apply plugin: 'java'
apply plugin: 'maven-publish'
apply plugin: 'whitesource'
 }

